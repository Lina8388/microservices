package ru.team.up.teamup;

/*import lombok.extern.slf4j.Slf4j;
import org.junit.platform.engine.discovery.DiscoverySelectors;
import org.junit.platform.launcher.Launcher;
import org.junit.platform.launcher.LauncherDiscoveryRequest;
import org.junit.platform.launcher.core.LauncherDiscoveryRequestBuilder;
import org.junit.platform.launcher.core.LauncherFactory;
import org.junit.platform.launcher.listeners.SummaryGeneratingListener;*/

//@Slf4j
public class TestLauncher {

    //public static void main(String[] args) {

/*        Launcher launcher = LauncherFactory.create();
        SummaryGeneratingListener summaryGeneratingListener = new SummaryGeneratingListener();

        LauncherDiscoveryRequest launcherDiscoveryRequest = LauncherDiscoveryRequestBuilder
                .request()
                .selectors(DiscoverySelectors.selectPackage(TestLauncher.class.getPackageName()))
                .build();

        launcher.execute(launcherDiscoveryRequest, summaryGeneratingListener);

        log.debug("Test summary: {}", summaryGeneratingListener.getSummary());*/
    //}

}
